# StudyPilot 项目开发规范

本文件适用于 StudyPilot 仓库根目录及其所有子目录。后续所有 Codex 开发任务都必须遵守本规范；若子目录未来存在更具体的 `AGENTS.md`，其规则只能在本文件基础上补充或收紧，不得违反本文件确定的项目定位、技术栈和安全边界。

## 1. 项目定位

StudyPilot 是一个供本科生长期学习和实践 AI 应用开发的项目。

项目代码不仅要能够运行，还应便于阅读、讲解、调试和逐步演进。实现时优先帮助学习者理解完整链路，避免用过度封装隐藏关键过程。

## 2. V1 技术栈

- 后端：Python、FastAPI
- 前端：Vue 3、Vite
- 关系型数据：SQLite、SQLAlchemy
- 向量存储：Chroma
- 文档解析：PyMuPDF、python-docx

未经用户明确同意，不得替换上述技术栈，也不得并行引入功能重叠的框架、数据库、ORM、向量数据库或文档解析库。

### V1 的 RAG 限制

V1 暂时禁止使用 LangChain 抽象核心 RAG 流程。文档解析、文本清洗、分块、向量化、入库、检索、上下文组装、模型调用和引用整理等核心步骤应以清晰的项目代码显式实现，让学习者能够直接追踪数据如何流动。

若第三方模型 SDK 仅用于访问模型服务，可以在确有必要且获得用户同意后引入；但不得用它重新封装或隐藏核心 RAG 流程。

## 3. 开发原则

1. 优先选择简单、清晰、可维护、易测试的实现。
2. 不得擅自引入微服务、事件总线、消息队列、复杂领域分层、插件系统等超出当前需求的架构。
3. 不得擅自替换已经确定的技术栈。
4. 不得在代码、测试、示例、文档、日志或提交内容中写入 API Key、令牌、密码等敏感信息。
5. 敏感配置必须通过环境变量读取，并由 `.env` 在本地提供；`.env` 不得提交到版本库。需要说明变量时，只能提供无真实秘密的 `.env.example`。
6. 任何新增第三方依赖都必须对当前任务确实必要。引入前先确认标准库或现有依赖无法合理完成任务，并在任务总结中说明用途。
7. 修改代码前必须先阅读相关实现、测试、配置、接口和文档，理解现状后再动手。
8. 禁止与当前任务无关的大规模重构、批量格式化、重命名或目录调整。
9. 保持修改范围小而完整；优先增量实现，不为假设中的未来需求提前设计。
10. 新代码应使用清楚的命名和必要的类型信息。注释解释原因、边界或非显然决策，不重复代码表面含义。

## 4. 架构边界

V1 采用单体前后端分离结构：Vue 3 单页应用通过 HTTP API 调用 FastAPI；FastAPI 内部以轻量模块划分接口、业务流程、RAG 流程和数据访问；SQLite/SQLAlchemy 保存结构化数据；Chroma 保存文本块、向量及检索所需元数据。

具体边界、数据流和暂未确定的事项以 [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md) 为准。实现新功能前，应确认它符合该文档；如果任务确实改变架构，必须同步更新架构文档，并在总结中明确说明。

## 5. 每次修改前的工作方式

1. 阅读本文件和任务涉及目录中更具体的 `AGENTS.md`。
2. 阅读 `docs/ARCHITECTURE.md`、相关源码、测试、配置和已有接口，不凭文件名猜测行为。
3. 检查当前工作区状态，保留用户已有改动，不覆盖、不回退、不顺手修改无关内容。
4. 明确当前任务的最小完成范围、可验证结果和可能受影响的模块。
5. 若任务过大，自行拆成可独立验证的小任务，逐步完成；不要一次修改整个项目。
6. 若关键需求存在多种会显著改变产品行为或数据结构的解释，先向用户说明分歧并请求确认。

## 6. 实现约定

### 后端

- FastAPI 路由负责 HTTP 输入输出、校验和状态码，不承载完整业务流程。
- 业务服务负责用例编排；RAG 各阶段保持可单独阅读、调用和测试。
- SQLAlchemy 模型与数据库访问保持集中，避免在路由中散落 SQL 或提交事务。
- 对外部模型、嵌入服务和文件输入设置清晰的错误边界，不向客户端泄露密钥、内部路径或完整异常堆栈。
- 公共函数和跨模块接口尽量提供 Python 类型标注；数据校验优先使用 FastAPI 已采用的模型机制。

### 前端

- 使用 Vue 3 和 Vite，组件保持职责单一。
- 页面展示、状态处理和 API 调用适度分离，但不为简单页面创建多余抽象层。
- 明确处理加载中、空数据、成功和失败状态；不得静默吞掉请求错误。
- API 地址等环境差异通过 Vite 环境配置管理，不硬编码秘密或部署环境专属值。

### RAG 与数据

- 核心 RAG 流程必须显式、可追踪、可测试，不使用 LangChain 代替流程实现。
- SQLite 中的文档记录与 Chroma 中的文本块必须通过稳定的文档标识关联。
- 文本块应保留回答溯源所需的最小元数据，例如文档标识、来源文件名、页码或段落位置。
- 写入、重试和删除应考虑 SQLite 与 Chroma 之间的一致性；失败必须可见且可诊断。
- PDF 使用 PyMuPDF 解析，DOCX 使用 python-docx 解析；不要仅为同类格式处理再引入重复依赖。

## 7. 测试与验证

- 每次代码修改都必须运行与改动直接相关的测试；影响公共流程时，再运行范围更大的测试。
- 新增行为或修复缺陷时，应补充能够证明行为的测试；不要只验证“没有抛异常”。
- 发现测试、类型检查、构建、启动或运行报错时，先定位并修复本次修改导致的问题，再宣告任务完成。
- 不得为了让检查通过而删除测试、降低断言、屏蔽异常或无解释地跳过检查。
- 若仓库尚无对应测试设施，进行可行的最小验证，并如实说明未能自动验证的部分；引入新的测试依赖仍须遵守依赖必要性原则。

## 8. 任务完成标准

每一个任务完成后都必须：

1. 运行相关测试。
2. 检查测试、构建或运行过程中的报错。
3. 修复与本次任务有关的失败，并重新验证。
4. 总结修改过的文件及各自用途。
5. 用适合本科生理解的语言解释关键代码和设计选择。
6. 给出可以照着执行的手动验证步骤和预期结果。
7. 告诉用户本阶段应该掌握的知识点。

最终回复还应明确列出实际运行过的检查及其结果。若某项无法运行，必须说明原因、已做的替代验证和剩余风险，不得把未运行描述为已通过。

## 9. 明确禁止的行为

- 未经授权实现当前任务范围之外的业务功能。
- 把秘密写进源码或提交 `.env`。
- 使用 LangChain 抽象 V1 核心 RAG 流程。
- 为追求“高级”而引入不必要的层、模式或基础设施。
- 在不了解相关文件的情况下直接修改。
- 覆盖、回退或整理用户的无关改动。
- 在没有验证证据时声称任务已经完成或测试已经通过。
